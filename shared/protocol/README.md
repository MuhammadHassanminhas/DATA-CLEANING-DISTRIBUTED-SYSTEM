# Distributed Communication Protocol

This directory defines the communication contract between the Coordinator, Workers, and other services.

The protocol documentation is the authoritative specification for:

- Worker lifecycle
- Task lifecycle
- Message flow
- State transitions

All implementations must follow these specifications.

Implementation details belong in the Coordinator and Worker services, not in this directory.