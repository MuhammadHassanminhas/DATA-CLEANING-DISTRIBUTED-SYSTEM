# Shared Package

This package contains code and documentation shared between the Coordinator, Worker, and Dashboard services.

It must not contain service-specific business logic.

Contents include:

- Shared models
- Enumerations
- Protocol definitions
- Constants
- Configuration schemas

The shared package acts as the single source of truth for communication contracts across the distributed system.