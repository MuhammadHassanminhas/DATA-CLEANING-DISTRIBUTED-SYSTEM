# Dev/test dependencies (not shipped in any service image).
pytest>=8,<9
httpx>=0.27,<1          # required by fastapi.testclient.TestClient
pip-audit>=2.7,<3       # dependency vulnerability scan (CI)
